
1.本工程CAN模块采用中断方式接收CAN总线中的数据，//并将数据通过串口发送给PC
  (1)使用的CAN通信速率为250k，SCI波特率为9600
  (2)程序通过接收can总线信号控制风扇运行。
  (3)本程序控制4路IO口，是控制4个风扇运行。
  //(3)MSCAN模块采用中断方式接收数据，然后通过SCI1向PC机发送接收到的内容
2.硬件连接
    //通过串口线，连接SCI1和PC串口.
    can线和控制继电器的IO口。
3.目的
     初步掌握利用CAN模块进行串行通信的基本知识.